[package]
name = "fishing-conformance-oracle"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
fishing = { version = "=0.2.0", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["float_roundtrip"] }
